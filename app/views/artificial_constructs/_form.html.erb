<%= form_with(model: artificial_construct, local: true) do |form| %>
  <% if artificial_construct.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artificial_construct.errors.count, "error") %> prohibited this artificial_construct from being saved:</h2>

      <ul>
      <% artificial_construct.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
